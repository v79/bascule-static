<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>{{ siteName }}</title>
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.7.1/css/bulma.min.css">
	<script defer src="https://use.fontawesome.com/releases/v5.1.0/js/all.js"></script>
</head>
<body>
<section class="section">
	<div class="container">
		<h1 class="title">
			{{ siteName }}
		</h1>

		<div class="content">
			<div class="box">
				{{#forEach basculePosts "2" }}
				<div class="content">
					<small>{{ this.date}}</small> <a href="{{this.url}}" title="{{this.title}}">{{ this.title }}</a><br/>
					{{ this.getSummary }}
				</div>
				{{/forEach}}
			</div>

			<ul>{{#forEach basculePosts }}
				<li>{{ this.date }} - <a href="{{this.url}}" title="{{this.title}}">{{ this.title }}</a>
					{{{ this.getSummary }}}
				</li>
				{{/forEach}}
			</ul>
		</div>
	</div>
</section>
</body>
</html>
